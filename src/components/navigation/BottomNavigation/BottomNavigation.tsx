import styles from "./BottomNavigation.module.css";
import { Box } from "@mantine/core";

const BottomNavigation = () => {
  return <Box className={styles.wrapper}>BottomNavigation</Box>;
};

export default BottomNavigation;
